[project]
name = "imprint-tests"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "claude-agent-sdk",
    "pytest",
    "pytest-asyncio",
    "pytest-xdist",
    "anyio",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["."]
